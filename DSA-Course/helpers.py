# returns pair
def pair(x, y):
    return ("("+str(x)+", "+str(y)+")")

# takes str and puts to array of chars format
def chArr(str):
    charArray = []
    for x in range (0, len(str)):
        charArray.append(str[x])

    return charArray



