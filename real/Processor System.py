# go through ability and put it into max heap
# counter = 0
# while process > 0:
#   popped = heapq.heappop()
#   process -= abs(popped)
#   heapq.heappush(floor(abs(popped)/2))
#   counter += 1
# return counter



